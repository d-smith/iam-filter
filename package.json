{
  "name": "iam-filter",
  "version": "0.0.1",
  "description": "Filter a CFN template to include only Role and Policy resources",
  "main": "iamfilter.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "yaml-cfn": "^0.2.0"
  }
}
